<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext liberado so para o backend local, e so no build de debug.
  O android:usesCleartextTraffic global abriria HTTP para qualquer dominio; aqui a lista e
  fechada: 10.0.2.2 (o host visto do emulador) e localhost.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>

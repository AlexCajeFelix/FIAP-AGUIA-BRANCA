# Perfil dos testes de integracao. O schema (indices e validadores $jsonSchema) vem do
# MongoSchemaInitializer, como em producao — e o objetivo e provar que ele cobre as mesmas
# garantias que os CHECK e UNIQUE da V1 davam.
app.jwt.secret=segredo-de-teste-com-mais-de-32-bytes-para-hs256
app.rate-limit.ip.capacity=5
app.rate-limit.ip.duration-minutes=1
app.rate-limit.email.capacity=3
app.rate-limit.email.duration-minutes=15
